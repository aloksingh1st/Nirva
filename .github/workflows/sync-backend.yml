name: Sync backend with monorepo

on:
  workflow_dispatch:
  repository_dispatch:
  schedule:
    - cron: '0 0 * * *'

jobs:
  sync-backend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout monorepo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Add and fetch backend repo
        env:
          TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        run: |
          git remote add backend-repo https://x-access-token:${TOKEN}@github.com/aloksingh1st/auth-microservice.git
          git fetch backend-repo main --tags

      - name: Merge backend into monorepo/backend
        run: |
          git subtree pull --prefix=backend backend-repo main --squash --allow-unrelated-histories -m "chore: sync backend repo"

      - name: Push changes to monorepo
        run: |
          git push origin main
